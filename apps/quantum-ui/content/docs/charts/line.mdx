---
title: Line Charts
description: Line chart component with optional area fill. Supports curved and linear interpolation, stacking, and gradient fills.
---

<LineChartDefaultDemo />

## Examples

### Fill Style

Toggle between fill styles and color sets. Gradient fill fades from the line color to transparent.

<LineChartFillDemo />

### Interpolation

Switch between curve types: natural (smooth), linear (straight), step, and monotone.

<LineChartInterpolationDemo />

### Stacked

Multiple series stacked on top of each other. Toggle stacking on and off.

<LineChartStackedDemo />

### Legend

Show or hide a legend identifying each data series.

<LineChartLegendDemo />

## API Reference

| Prop | Type | Default |
| --- | --- | --- |
| `data` | `Record<string, unknown>[]` | — |
| `categories` | `string[]` | — |
| `index` | `string` | — |
| `config` | `ChartConfig` | — |
| `fill` | `"none" \| "solid" \| "gradient"` | `"gradient"` |
| `interpolation` | `"curved" \| "linear" \| "step" \| "monotone"` | `"curved"` |
| `stacked` | `boolean` | `false` |
| `showBackground` | `boolean` | `true` |
| `showGrid` | `boolean` | `true` |
| `showXAxis` | `boolean` | `true` |
| `showYAxis` | `boolean` | `false` |
| `showLegend` | `boolean` | `false` |
| `showTooltip` | `boolean` | `true` |
