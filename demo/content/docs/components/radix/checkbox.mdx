---
title: Checkbox
description: A control that allows the user to toggle between checked and unchecked.
featured: true
component: true
---

<CheckboxDemoPreview />

## Usage

```tsx
import { Checkbox } from "@/components/ui/checkbox"
```

```tsx
<Checkbox label="Accept terms and conditions" />
```

## Examples

### With Label

Use the `label` prop for a quick inline label.

<CheckboxWithLabelPreview />

### With Description

Use the `description` prop for helper text below the label.

<CheckboxDescriptionPreview />

### Card

Use the `card` prop to wrap in a bordered card that highlights when checked.

<CheckboxCardPreview />

### Invalid

Use the `invalid` prop to indicate a validation error.

<CheckboxInvalidPreview />

### Disabled

<CheckboxDisabledPreview />

### Standalone

Without the `label` prop, the checkbox renders on its own.

<CheckboxStandalonePreview />

## Demo

See the [interactive demo](/demos/checkbox) for a full visual reference.

## API Reference

| Prop | Type | Default |
| --- | --- | --- |
| `label` | `string` | - |
| `description` | `string` | - |
| `invalid` | `boolean` | `false` |
| `card` | `boolean` | `false` |
| `checked` | `boolean` | - |
| `defaultChecked` | `boolean` | - |
| `onCheckedChange` | `(checked: boolean) => void` | - |
| `disabled` | `boolean` | `false` |
