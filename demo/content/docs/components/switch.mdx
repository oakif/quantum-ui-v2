---
title: Switch
description: A toggle control for on/off states, with optional label, description, and card layout.
featured: true
component: true
---

<SwitchDemoPreview />

## Usage

```tsx
import { Switch } from "@/components/ui/switch"
```

```tsx
<Switch checked={checked} onCheckedChange={setChecked} />
```

## Examples

### Basic

A standalone switch toggle.

<SwitchDemoPreview />

### With Label

Use the `label` prop to add a clickable label.

<SwitchWithLabelPreview />

### With Description

Add `description` for additional context below the label.

<SwitchWithDescriptionPreview />

### Reverse Layout

Use `reverse` to put the label on the left and switch on the right. Useful for settings-style layouts.

<SwitchReversePreview />

### Card

Use `card` for a bordered card layout. Combine with `reverse` for a settings panel look.

<SwitchCardPreview />

### Sizes

Available in `sm`, `default`, and `lg`.

<SwitchSizesPreview />

### Disabled

<SwitchDisabledPreview />

## API Reference

| Prop | Type | Default |
| --- | --- | --- |
| `checked` | `boolean` | - |
| `onCheckedChange` | `(checked: boolean) => void` | - |
| `label` | `string` | - |
| `description` | `string` | - |
| `card` | `boolean` | `false` |
| `reverse` | `boolean` | `false` |
| `size` | `"sm" \| "default" \| "lg"` | `"default"` |
| `disabled` | `boolean` | `false` |
| `className` | `string` | - |
